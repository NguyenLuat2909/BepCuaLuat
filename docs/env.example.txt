# =====================================================
# Bếp của Luật — Biến môi trường mẫu
# Đổi tên thành ".env.example" khi nộp báo cáo.
# =====================================================

# --- Database (PostgreSQL) ---
# Anything: TỰ ĐỘNG. Bên ngoài: Neon, Supabase Postgres, Railway, hoặc VPS
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# --- Auth ---
AUTH_SECRET=thay-bang-chuoi-ngau-nhien-tu-openssl-rand-hex-32
AUTH_URL=http://localhost:3000

# --- App URL gốc ---
NEXT_PUBLIC_CREATE_APP_URL=http://localhost:3000

# --- Google Gemini AI ---
# Anything: TỰ ĐỘNG. Bên ngoài: API key từ Google AI Studio
GEMINI_API_KEY=

# --- Upload ảnh ---
# Anything: TỰ ĐỘNG qua useUpload. Bên ngoài: Cloudinary / S3 / Uploadcare
NEXT_PUBLIC_UPLOADCARE_PUBLIC_KEY=

# --- Postgres (cho docker-compose) ---
POSTGRES_PASSWORD=strong-random-password
